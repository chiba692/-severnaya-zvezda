const {verifySession}=require('./_auth');const {validate,type,dateInfo}=require('./_schedule');const reply=(statusCode,body)=>({statusCode,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'},body:JSON.stringify(body)});const SERVICES=['exam','ultrasound','xray','vaccination','tests','inpatient','other'];const clean=(v,n=200)=>String(v||'').trim().slice(0,n);function cleanDetails(s,d){d=d&&typeof d==='object'?d:{};if(s==='vaccination')return{vaccine_type:clean(d.vaccine_type,30),last_vaccine_date:d.last_vaccine_date||null};if(['ultrasound','xray'].includes(s))return{area:clean(d.area,120)};if(s==='tests')return{test_type:clean(d.test_type,30)};if(s==='inpatient')return{care:clean(d.care,500)};if(s==='other')return{reason:clean(d.reason,200)};return{}}
exports.handler=async event=>{if(event.httpMethod!=='POST')return reply(405,{error:'Method not allowed'});if(!verifySession(event))return reply(401,{error:'Требуется авторизация'});try{const b=JSON.parse(event.body||'{}'),url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SECRET_KEY;if(!url||!key)throw Error('Supabase env missing');const h={'Content-Type':'application/json',apikey:key,Authorization:`Bearer ${key}`};if(b.action==='delete'){if(!b.id)return reply(400,{error:'Не указана заявка'});let r=await fetch(`${url}/rest/v1/bookings?id=eq.${encodeURIComponent(b.id)}`,{method:'DELETE',headers:h});if(!r.ok)throw Error(await r.text());return reply(200,{ok:true})}
if(b.action==='status'){if(!b.id||!['new','accepted','completed','rejected','no_show'].includes(b.status))return reply(400,{error:'Некорректный статус'});let r=await fetch(`${url}/rest/v1/bookings?id=eq.${encodeURIComponent(b.id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify({status:b.status})});if(!r.ok)throw Error(await r.text());return reply(200,{ok:true})}
if(['create','edit'].includes(b.action)){const owner_name=clean(b.owner_name,100),phone=clean(b.phone,40),pet=clean(b.pet,100),pet_species=clean(b.pet_species,20)||'other',pet_age=clean(b.pet_age,40),service=clean(b.service,30)||'exam',comment=clean(b.comment,700),service_details=cleanDetails(service,b.service_details);if(!owner_name||!phone||!pet||!SERVICES.includes(service))return reply(400,{error:'Заполните обязательные поля'});let booking_date=String(b.booking_date||''),booking_time=String(b.booking_time||''),stay_start=b.stay_start?String(b.stay_start):null,stay_end=b.stay_end?String(b.stay_end):null;const inpatient=service==='inpatient';if(inpatient){if(!dateInfo(stay_start)||!dateInfo(stay_end)||stay_end<stay_start)return reply(400,{error:'Проверьте даты стационара'});booking_date=stay_start;booking_time='00:00'}else{const err=validate(booking_date,booking_time,true);if(err)return reply(400,{error:err});let qr=await fetch(`${url}/rest/v1/bookings?booking_date=eq.${encodeURIComponent(booking_date)}&booking_time=eq.${encodeURIComponent(booking_time)}&select=id`,{headers:h});if(!qr.ok)throw Error(await qr.text());let conflicts=(await qr.json()).filter(x=>String(x.id)!==String(b.id||''));if(conflicts.length)return reply(409,{error:'Это время уже занято'})}
const payload={owner_name,phone,pet,pet_species,pet_age:pet_age||null,booking_date,booking_time,comment:comment||null,booking_type:type(booking_date),service,service_details,stay_start:inpatient?stay_start:null,stay_end:inpatient?stay_end:null};let r;if(b.action==='create'){payload.status='accepted';r=await fetch(`${url}/rest/v1/bookings`,{method:'POST',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify(payload)})}else{if(!b.id)return reply(400,{error:'Не указана заявка'});r=await fetch(`${url}/rest/v1/bookings?id=eq.${encodeURIComponent(b.id)}`,{method:'PATCH',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify(payload)})}if(!r.ok){let t=await r.text();if(r.status===409||t.includes('23505'))return reply(409,{error:'Это время уже занято'});throw Error(t)}return reply(200,{ok:true})}return reply(400,{error:'Неизвестное действие'})}catch(e){console.error(e);return reply(500,{error:'Не удалось изменить заявку'})}};