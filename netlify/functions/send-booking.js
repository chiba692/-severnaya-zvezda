const webpush=require('web-push');const {validate,type,now,today,mins,dateInfo}=require('./_schedule');
const reply=(statusCode,body)=>({statusCode,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'},body:JSON.stringify(body)});
const SERVICES={exam:'Осмотр',ultrasound:'УЗИ',xray:'Рентген',vaccination:'Вакцинация',tests:'Анализы',inpatient:'Стационар',other:'Другое'};
const clean=(v,n=200)=>String(v||'').trim().slice(0,n);function cleanDetails(s,d){d=d&&typeof d==='object'?d:{};if(s==='vaccination')return{vaccine_type:clean(d.vaccine_type,30),last_vaccine_date:d.last_vaccine_date||null};if(['ultrasound','xray'].includes(s))return{area:clean(d.area,120)};if(s==='tests')return{test_type:clean(d.test_type,30)};if(s==='inpatient')return{care:clean(d.care,500)};if(s==='other')return{reason:clean(d.reason,200)};return{}}
exports.handler=async event=>{if(event.httpMethod!=='POST')return reply(405,{error:'Method not allowed'});try{const b=JSON.parse(event.body||'{}'),owner_name=clean(b.owner_name,100),phone=clean(b.phone,40),pet=clean(b.pet,100),pet_species=clean(b.pet_species,20)||'other',pet_age=clean(b.pet_age,40),service=clean(b.service,30)||'exam',comment=clean(b.comment,700),service_details=cleanDetails(service,b.service_details);if(!owner_name||!phone||!pet||!SERVICES[service])return reply(400,{error:'Заполните обязательные поля'});let booking_date=String(b.booking_date||''),booking_time=String(b.booking_time||''),stay_start=b.stay_start?String(b.stay_start):null,stay_end=b.stay_end?String(b.stay_end):null;const inpatient=service==='inpatient';if(inpatient){if(!dateInfo(stay_start)||!dateInfo(stay_end)||stay_end<stay_start||stay_start<today())return reply(400,{error:'Проверьте даты стационара'});booking_date=stay_start;booking_time='00:00'}else{let err=validate(booking_date,booking_time);if(err)return reply(400,{error:err});if(booking_date===today()){const n=now();if(mins(booking_time)<=n.getHours()*60+n.getMinutes())return reply(400,{error:'Это время уже прошло'})}}
const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SECRET_KEY;if(!url||!key)throw Error('Supabase env missing');const h={'Content-Type':'application/json',apikey:key,Authorization:`Bearer ${key}`};if(!inpatient){const q=await fetch(`${url}/rest/v1/bookings?booking_date=eq.${encodeURIComponent(booking_date)}&booking_time=eq.${encodeURIComponent(booking_time)}&select=id&limit=1`,{headers:h});if(!q.ok)throw Error(await q.text());if((await q.json()).length)return reply(409,{error:'Это время уже занято. Выберите другое.'})}
const payload={owner_name,phone,pet,pet_species,pet_age:pet_age||null,booking_date,booking_time,comment:comment||null,booking_type:type(booking_date),status:'new',service,service_details,stay_start:inpatient?stay_start:null,stay_end:inpatient?stay_end:null};const ins=await fetch(`${url}/rest/v1/bookings`,{method:'POST',headers:{...h,Prefer:'return=minimal'},body:JSON.stringify(payload)});if(!ins.ok){const t=await ins.text();if(ins.status===409||t.includes('23505'))return reply(409,{error:'Это время уже занято. Выберите другое.'});throw Error(t)}
let sent=false,pub=process.env.VAPID_PUBLIC_KEY,priv=process.env.VAPID_PRIVATE_KEY;if(pub&&priv){try{const sr=await fetch(`${url}/rest/v1/push_subscriptions?select=id,subscription`,{headers:h});if(sr.ok){webpush.setVapidDetails('https://sevzdezd.netlify.app',pub,priv);const when=inpatient?`${stay_start} — ${stay_end}`:`${booking_date} в ${booking_time}`,pp=JSON.stringify({title:'🐾 Новая заявка — '+SERVICES[service],body:`${owner_name}: ${pet}, ${when}`,url:'/admin.html'});for(const row of await sr.json()){try{await webpush.sendNotification(row.subscription,pp,{TTL:300,urgency:'high'});sent=true}catch(e){if(e.statusCode===404||e.statusCode===410)await fetch(`${url}/rest/v1/push_subscriptions?id=eq.${encodeURIComponent(row.id)}`,{method:'DELETE',headers:h}).catch(()=>{})}}}}catch(e){console.error('Push error',e)}}return reply(200,{ok:true,push:sent})}catch(e){console.error(e);return reply(500,{error:'Ошибка сервера'})}};