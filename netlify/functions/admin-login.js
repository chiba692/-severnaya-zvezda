const crypto=require('crypto');
const out=(statusCode,body,headers={})=>({statusCode,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store',...headers},body:JSON.stringify(body)});
exports.handler=async event=>{if(event.httpMethod!=='POST')return out(405,{error:'Method not allowed'});try{const {login,password}=JSON.parse(event.body||'{}'),adminLogin=process.env.ADMIN_LOGIN,adminPassword=process.env.ADMIN_PASSWORD,secret=process.env.ADMIN_SESSION_SECRET;if(!adminLogin||!adminPassword||!secret)return out(500,{error:'Сервер авторизации не настроен'});if(login!==adminLogin||password!==adminPassword)return out(401,{error:'Неверный логин или пароль'});const payload=Buffer.from(JSON.stringify({role:'admin',exp:Date.now()+604800000})).toString('base64url'),sig=crypto.createHmac('sha256',secret).update(payload).digest('base64url'),token=`${payload}.${sig}`;return out(200,{ok:true},{'Set-Cookie':`admin_session=${token}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=604800`})}catch(e){console.error(e);return out(400,{error:'Некорректный запрос'})}};
