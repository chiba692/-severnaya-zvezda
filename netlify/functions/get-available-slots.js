const {S,mins,dateInfo,now,today,type}=require('./_schedule');const reply=(statusCode,body)=>({statusCode,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'},body:JSON.stringify(body)});
exports.handler=async event=>{if(event.httpMethod!=='GET')return reply(405,{error:'Method not allowed'});try{const date=(event.queryStringParameters||{}).date,di=dateInfo(date);if(!di)return reply(400,{error:'Некорректная дата'});const td=today(),bt=type(date);if(date<td)return reply(200,{date,type:bt,slots:[]});const sch=S[di.x.getUTCDay()];if(!sch)return reply(200,{date,type:bt,slots:[]});const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_SECRET_KEY;if(!url||!key)throw Error('Supabase env missing');const r=await fetch(`${url}/rest/v1/bookings?booking_date=eq.${encodeURIComponent(date)}&service=neq.inpatient&select=booking_time`,{headers:{apikey:key,Authorization:`Bearer ${key}`}});if(!r.ok)throw Error(await r.text());const busy=new Set((await r.json()).map(x=>String(x.booking_time).slice(0,5)));let slots=[];for(let m=mins(sch[0]);m<mins(sch[1]);m+=20){const t=String(Math.floor(m/60)).padStart(2,'0')+':'+String(m%60).padStart(2,'0');if(!busy.has(t))slots.push(t)}if(date===td){const n=now(),cur=n.getHours()*60+n.getMinutes();slots=slots.filter(t=>mins(t)>cur)}return reply(200,{date,type:bt,slots})}catch(e){console.error(e);return reply(500,{error:'Ошибка сервера'})}};