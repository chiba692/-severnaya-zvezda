const {reply,env,isAdmin}=require('./_util');
exports.handler=async event=>{if(!isAdmin(event))return reply(401,{error:'Требуется авторизация'});try{const {url,headers}=env();if(event.httpMethod==='GET'){const r=await fetch(`${url}/rest/v1/reviews?select=*&order=created_at.desc`,{headers});if(!r.ok)throw Error(await r.text());return reply(200,{reviews:await r.json()})}if(event.httpMethod==='POST'){const b=JSON.parse(event.body||'{}');if(!b.id)return reply(400,{error:'Не указан отзыв'});if(b.action==='delete'){const r=await fetch(`${url}/rest/v1/reviews?id=eq.${encodeURIComponent(b.id)}`,{method:'DELETE',headers});if(!r.ok)throw Error(await r.text());return reply(200,{ok:true})}if(['publish','hide'].includes(b.action)){const r=await fetch(`${url}/rest/v1/reviews?id=eq.${encodeURIComponent(b.id)}`,{method:'PATCH',headers:{...headers,Prefer:'return=minimal'},body:JSON.stringify({is_published:b.action==='publish'})});if(!r.ok)throw Error(await r.text());return reply(200,{ok:true})}return reply(400,{error:'Неизвестное действие'})}return reply(405,{error:'Method not allowed'})}catch(e){console.error(e);return reply(500,{error:'Не удалось изменить отзыв'})}};
