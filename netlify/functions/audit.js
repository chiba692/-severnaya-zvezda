const {reply,env,requireAdmin}=require('./_util');
exports.handler=async event=>{if(event.httpMethod!=='GET')return reply(405,{error:'Method not allowed'});const a=requireAdmin(event);if(!a.ok)return reply(a.status,{error:a.error});try{const id=Number(event.queryStringParameters?.booking_id);if(!id)return reply(400,{error:'Не указана запись'});const {url,headers}=env();const r=await fetch(`${url}/rest/v1/booking_audit?booking_id=eq.${id}&select=id,booking_id,actor,action,details,created_at&order=created_at.desc&limit=100`,{headers});if(!r.ok)throw Error(await r.text());return reply(200,{items:await r.json()})}catch(e){console.error(e);return reply(500,{error:'Не удалось загрузить историю'})}};
